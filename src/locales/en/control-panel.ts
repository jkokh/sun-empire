import { ControlPanelTranslations } from '../types';

const controlPanel: ControlPanelTranslations = {
    title: 'Control Panel',
    content: 'this is the content',
    menu: {
        categories: 'Categories',
        products: 'Products'
    }
};

export default controlPanel;
