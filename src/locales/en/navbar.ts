import { NavbarTranslations } from '../types';

const navbar: NavbarTranslations = {
    dashboard: 'Dashboard',
    createAccount: 'Create Account',
    contacts: 'Contacts',
    signIn: 'Sign In',
    cp: 'Admin'
};

export default navbar;
